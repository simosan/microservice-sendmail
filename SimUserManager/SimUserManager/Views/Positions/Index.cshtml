@model IEnumerable<SimUserManager.Models.Positions>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PositionName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rankvalue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PositionName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rankvalue)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PositionNo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PositionNo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PositionNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
